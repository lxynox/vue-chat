import * as types from './mutation-types'

export default {
	[types.JOIN_CHAT] (state, {}) {

	},
	[types.LEAVE_CHAT] (state, {}) {

	},
	[types.Typing] (state, {}) {

	},
	[types.STOP_TYPING] (state, {}) {

	},
	[types.CHANGE_CHAT_STATUS] (state, {}) {

	}
}
